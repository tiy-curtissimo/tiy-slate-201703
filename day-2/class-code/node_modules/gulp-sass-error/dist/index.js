"use strict";
var gulp_util_1 = require('gulp-util');
function gulpSassError(fail) {
    return function (error) {
        var message = new gulp_util_1.PluginError('sass', error.messageFormatted).toString();
        // Throw error instead of logging it if module is set to fail on error
        if (fail) {
            throw message;
        }
        process.stderr.write(message + '\n');
        this.emit('end');
    };
}
exports.gulpSassError = gulpSassError;
